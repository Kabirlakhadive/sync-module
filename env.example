# TrueNAS API (Derived from video-vault-web-app stack)
# Using internal URL (requires --add-host truenas.portal:host-gateway in Docker)
TRUENAS_API_URL=https://truenas.portal/api/v2.0
# The token from your stack is Basic Auth (Base64 encoded), so we prefix with "Basic "
TRUENAS_API_TOKEN="dnZhZG1pbjp2dmFkbWluQDIwMjU="

# App (Running on Port 3003)
NEXTAUTH_URL=http://localhost:3003
NEXT_PUBLIC_APP_URL=http://localhost:3003
